/* 
 * File:   main.c
 * Author: NOTEBOOKTJ
 *
 * Created on 12 de Maio de 2017, 19:08
 */

#include <stdio.h>
#include <stdlib.h>

/*
 * 
 */
int main(int argc, char** argv) {
    int n, i = 0, j, aux;
    scanf("%d", &n);
   
    while (n != 0) {
        if (i != 0) scanf("%d", &n);
        j = 0;
        while (j < n){
            int x[n], y[n], total[n];
            scanf("%d %d", &x[j], &y[j]);
            total;
            j++;
        }
        i++;
    }
    return (EXIT_SUCCESS);
}

